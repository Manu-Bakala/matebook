{{ form_start(form_post) }}
    {{ form_widget(form_post) }}

    <input type="submit" class="btn btn-primary btn-block mt-5" value="{{ submitButtonText ?? 'Ajouter' }}" formnovalidate>
{{ form_end(form_post) }}