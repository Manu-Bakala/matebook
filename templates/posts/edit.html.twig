{% extends 'layouts/base.html.twig' %}

{% block title %}Metabook - Edit Post {{ post.title }} {% endblock %}

{% block body %}
    <h1>Edit Post {{ post.title }}</h1>

    {{ include('posts/_form.html.twig', {submitButtonText:'Update Post'}) }}

    <a href="{{ path('app_home') }}">Go back</a>
{% endblock %}