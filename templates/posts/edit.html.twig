{% extends 'layouts/base.html.twig' %}

{% block title %}Metabook - Modifiez la publication{{ post.title }} {% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-6 col-md-8 col-sm-12 mx-auto border rounded mt-5 p-5 shadow-lg bg-form">
            <h1 class="text-center mb-4">
                <img src="{{ asset('build/images/logo_matebook.png') }}" width="50px" height="50px">
                MateBook<br>
                <span class="sub-title">Modifez votre publication</span>
            </h1>
            <p class="text-center text-muted mb-4">
                <i class="far fa-newspaper"></i>&nbsp;
                 "{{ post.title }}"
            </p>

            {{ include('posts/_form.html.twig', {submitButtonText:'Modifier'}) }}
        </div>
    </div>
{% endblock %}